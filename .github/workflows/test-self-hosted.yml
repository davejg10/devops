name: Test self-hosted runners

on:
  workflow_dispatch:
    
jobs:
  test:
    runs-on: self-hosted

    steps:

      - run: |
          echo $APP_ID
          echo $APP_PRIVATE_KEY
          echo $RUNNER_SCOPE
          
        name: Execute az cli sample commands