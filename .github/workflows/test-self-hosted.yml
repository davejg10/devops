name: Test self-hosted runners

on:
  workflow_dispatch:
    
jobs:
  test:
    runs-on: self-hosted
    steps:
      - run: |
          cat ~/.bashrc
          java -version

          source ~/.bashrc

          java -version
        name: Execute az cli sample commands